<%= turbo_frame_tag workout_exercise_exercise_sets_path(@workout, @exercise) do %>
    <% @exercise_sets.map do |exercise_set| %>
        <%= render "exercise_set", workout: @workout, exercise: @exercise, exercise_set: exercise_set %>
    <% end %>
<% end %>