<!DOCTYPE html>
<html>
  <head>
    <title>Workout Recorder</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-zinc-900 text-zinc-300 min-h-screen grid grid-rows-[auto_1fr_auto]">
    <%= render "header" %>

    <div class="px-6">
      <div class="my-6">
        <% if notice %>
          <p class="notice px-6"><%= notice %></p>
        <% elsif alert %>
          <p class="alert px-6"><%= alert %></p>
        <% end %>
      </div>

      <div class="relative">
        <div class="fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 bg-gray-600 z-20 max-h-[90vh]
        md:max-w-80 overflow-auto">
          <%= turbo_frame_tag "modal", class: "[&[busy]]:blur-sm [&[busy]+div]:block" %>
          <%= render "application/loading_spinner" %>
        </div>
      </div>    
      
      <%= yield %>
    </div>

    <div>
      <%= render "footer" %>
    </div>
  </body>
</html>
