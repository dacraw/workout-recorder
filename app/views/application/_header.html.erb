<nav>
    <div class="gap-x-10 hidden md:flex justify-end mr-6 mt-6">
        <%= link_to tag_selector_path, class: "#{local_assigns[:class_name]} flex gap-2 items-center", data: { turbo_frame: "modal"} do %>
            <i class="fa-regular fa-lightbulb"></i>
            <span>Suggestions</span>
        <% end %>
        <%= render "header_links" %>
        <%= render "user_session_links" %>
    </div>

    <%= render 'header_dropdown' %>
</nav> 
