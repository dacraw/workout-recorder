<%= link_to edit_workout_path(workout), class: "mr-4 md:mr-2", title: "Edit Workout" do %>
    <i class="fa-solid fa-pencil"></i>

    <% if local_assigns[:show_text] %>
        <span>Edit Workout</span>
    <% end %>
<% end %>